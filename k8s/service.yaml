apiVersion: v1
kind: Service
metadata:
  name: cloudops-service
  namespace: cloudops
spec:
  selector:
    app: cloudops-app
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30080
